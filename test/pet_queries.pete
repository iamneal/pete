InsertFish


.test.Fish
.test.Empty
INSERT INTO fish(
    species,
    cost
) VALUES(
    @species,
    @cost
)

GetCatByName
in: .test.CatName
out: .test.Cat
SELECT
    name,
    age,
    cost
FROM cats
WHERE
    name = @cat_name